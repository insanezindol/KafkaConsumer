spring:
  application:
    name: KafkaConsumer
  mvc:
    throw-exception-if-no-handler-found: true
  resources:
    add-mappings: false
  kafka:
    consumer:
      group-id: elasticsearch-group
    bootstrap-servers: docker_host:9092
  main:
    allow-bean-definition-overriding: true

server:
  port: 7777
  servlet:
    context-path: /
  max-http-header-size: 8KB
  tomcat:
    max-threads: 100
    max-swallow-size: 2MB
    max-http-form-post-size: 2MB
    accesslog:
      enabled: true
      prefix: access_log_kafkaconsumer
      pattern: "[%A] [%v] [%{yyyy-MM-dd HH:mm:ss}t]  [%a] [%h] [%S] [accesslog] [%m] [%r] [%{User-Agent}i]"
    basedir: E:\access_log
  error:
    whitelabel:
      enabled: false

elasticsearch:
  host: docker_host
  port: 9200